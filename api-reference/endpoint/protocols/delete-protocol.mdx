---
title: 'Delete Protocol'
openapi: 'DELETE /protocols/{protocolId}'
---

Remove a protocol from the OpenLoop Health Partners API. This action permanently deletes the protocol and its configuration.

**Important:** Deleting a protocol may affect existing patient enrollments. Ensure no active enrollments depend on this protocol before deletion.
