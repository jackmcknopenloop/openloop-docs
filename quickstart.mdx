---
title: 'Quickstart'
description: 'Get up and running with the OpenLoop Partners API in minutes'
---

# Your First Integration in 10 Minutes

Ready to transform your practice? This guide will have you connected and referring your first patient to specialized care in under 10 minutes.

## What You'll Need

- **API Key** - Get one from our partnership team
- **HTTP Client** - curl, Postman, or your favorite tool
- **Basic API Knowledge** - You've got this! 💪

## Step 1: Verify Your Connection

Let's start by checking that your API credentials are working:

```bash
# Production
curl -H "x-api-key: YOUR_API_KEY" \
     https://partners-api-sigma.vercel.app/health

# Staging (for testing)
curl -H "x-api-key: YOUR_API_KEY" \
     https://staging-partners-api-sigma.vercel.app/health
```

<CodeGroup>
```bash cURL
# Production
curl -H "x-api-key: YOUR_API_KEY" \
     https://partners-api-sigma.vercel.app/health

# Staging
curl -H "x-api-key: YOUR_API_KEY" \
     https://staging-partners-api-sigma.vercel.app/health
```

```javascript JavaScript
// Production
const response = await fetch('https://partners-api-sigma.vercel.app/health', {
  headers: {
    'x-api-key': 'YOUR_API_KEY'
  }
});

// Staging
const response = await fetch('https://staging-partners-api-sigma.vercel.app/health', {
  headers: {
    'x-api-key': 'YOUR_API_KEY'
  }
});

const data = await response.json();
console.log(data);
```

```python Python
import requests

# Production
response = requests.get(
    'https://partners-api-sigma.vercel.app/health',
    headers={'x-api-key': 'YOUR_API_KEY'}
)

# Staging
response = requests.get(
    'https://staging-partners-api-sigma.vercel.app/health',
    headers={'x-api-key': 'YOUR_API_KEY'}
)

print(response.json())
```
</CodeGroup>

You should receive a response like:
```json
{
  "status": "ok",
  "ts": 1640995200
}
```

## Step 2: Explore Available Programs

Before referring patients, let's see what programs are available:

```bash
curl -H "x-api-key: YOUR_API_KEY" \
     https://partners-api-sigma.vercel.app/programs
```

This will return a list of available medical programs your patients can access.

## Step 3: Create Your First Patient

Now let's create a patient record:

```bash
curl -X POST \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "patient@example.com",
    "first_name": "John",
    "last_name": "Doe",
    "phone": "+1234567890",
    "birth_date": "1990-01-01",
    "gender": "male",
    "sex": "male",
    "locations": [{
      "address_line_1": "123 Main St",
      "city": "Anytown",
      "state": "CA",
      "country": "US",
      "zip_code": "12345",
      "default": true,
      "type": "shipping"
    }]
  }' \
  https://partners-api-sigma.vercel.app/patients
```

## Step 4: Enroll Patient in a Program

Once you have a patient, you can enroll them in a medical program:

```bash
curl -X POST \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "program_id": 1,
    "stripe_customer_id": "cus_example123",
    "stripe_product_id": "prod_example456"
  }' \
  https://partners-api-sigma.vercel.app/patients/PATIENT_ID/enrollments
```

## Authentication Methods

The API supports two authentication methods:

<CardGroup cols={2}>
  <Card title="API Key" icon="key">
    Include your API key in the `x-api-key` header for simple authentication
  </Card>
  <Card title="Bearer Token" icon="shield">
    Use OAuth2-style bearer tokens in the `Authorization` header
  </Card>
</CardGroup>

## Next Steps

<CardGroup cols={3}>
  <Card title="API Reference" icon="code" href="/api-reference">
    Explore all available endpoints and schemas
  </Card>
  <Card title="Development Guide" icon="wrench" href="/development">
    Learn about error handling, rate limits, and best practices
  </Card>
  <Card title="Contact Support" icon="envelope" href="https://openloophealth.com/contact">
    Get help from our technical team
  </Card>
</CardGroup>
